using UnityEngine;

public class DebugButton : AnimatedButtonTrigger
{
    private void Start()
    {
        //activationEvent.AddListener(() => Debug.Log("Push"));
        //diactivationEvent.AddListener(() => Debug.Log("Pull"));
    }
}